const NoResult = ({ message }: { message: string }) => {
  return (
    <div className=" text-center my-32 text-gray-700 font-light text-sm">
      {message}
    </div>
  );
};

export default NoResult;
